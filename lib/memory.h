#ifndef _MEMORY_H
#define _MEMORY_H 1

#include <stdlib.h> /* malloc, calloc, realloc, etc. */
#include <string.h> /* memcpy, memcmp, memmove, etc. */

extern size_t PAGESIZE;
extern size_t CHONKSIZE;

#endif /* _MEMORY_H */
